# Resend (contact form): https://resend.com/docs
RESEND_API_KEY=

# Required for real delivery: verify your domain at https://resend.com/domains then set:
# RESEND_FROM="Portfolio <hello@your-domain.com>"
# Until then, `from` stays onboarding@resend.dev and Resend only sends `to` your Resend-account email.

# Inbound for the form is contactData.formInboxEmail in src/data/data.ts (keep in sync with Resend rules).
